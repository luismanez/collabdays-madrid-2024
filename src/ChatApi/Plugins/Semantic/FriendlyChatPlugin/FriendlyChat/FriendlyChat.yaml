name: FriendlyChat
template: |
  Question: {{$input}}
template_format: semantic-kernel
description: This function can help with any user's goal. This is the function that you MUST use if any other function suits better the user's goal.
input_variables:
  - name: input
    description: User input
    is_required: true
output_variable:
  description: The answer from the system.
execution_settings:
  default:
    temperature: 0.3